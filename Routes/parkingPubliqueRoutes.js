const express = require('express');
const parkingpubliqueController = require('../Controllers/parking_publiqueController');
const {  } = parkingpubliqueController;
const router = express.Router();


module.exports = router;