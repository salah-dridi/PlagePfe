const express = require('express');
const parkingpriveController = require('../Controllers/parking_priveController');
const {  } = parkingpriveController;
const router = express.Router();


module.exports = router;